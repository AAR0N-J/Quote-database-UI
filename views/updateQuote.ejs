<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
  <title> Update Quote </title>
</head>
<body class="text-center">
  <h1> Update Quote </h1>

  <form action="/updateQuote" method="POST">
    Quote:<br>
    <textarea name="quote"><%= quoteInfo[0].quote %></textarea><br>
    <input type="hidden" name="quoteId" value="<%= quoteInfo[0].quoteId %>">
    Author: 
    <select  name="authorId">
      <% for (i=0; i< authors.length;i++) { %>
      <option <%=(authors[i].authorId == quoteInfo[0].authorId)?"selected":""%> value="<%= authors[i].authorId %>"><%= authors[i].firstName %> <%= authors[i].lastName %> </option>
        <% } %>
    </select>
    Category:
    <select  name="category">
      <% for (i=0; i< categories.length;i++) { %>
      <option <%=(quoteInfo[0].category == categories[i].category)?"selected":""%> value="<%= categories[i].category %>"><%= categories[i].category %> </option>
        <% } %>
    </select>
          <br><br>
    Likes:
    <input type="number" name="likes" value="<%= quoteInfo[0].likes %>">
          <br>
    <button class="btn btn-secondary m-3"> Update Quote</button>
  </form>
  
</body>
</html>